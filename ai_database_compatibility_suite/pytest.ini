[pytest]
testpaths = tests
python_files = test_*.py
markers =
    live: calls live database/AI services or mutates test data
    database: requires PostgreSQL/Supabase
    rag: RAG compatibility tests
    coach: Coach compatibility tests
    verification: Verification compatibility tests
    e2e: end-to-end integration tests
