# Backend + AI runtime. Tested with the exact versions noted in docs/TESTING.md.
-r requirements-ai.txt

# HTTP layer
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.31
httpx>=0.27,<0.28
python-multipart>=0.0.9

# Auth + Storage (Supabase), Postgres access with real transactions
supabase>=2.4,<3
psycopg[binary,pool]>=3.2,<4
psycopg2-binary

# File pipeline: format-specific extraction (all in-process; nothing shells out)
python-docx>=1.1,<2            # .docx
python-pptx>=1.0,<2            # .pptx
openpyxl>=3.1,<4               # .xlsx (read-only, cached values; macros never executed)
defusedxml>=0.7,<1             # makes openpyxl reject XML entity attacks
xlrd>=2.0,<3                   # .xls
olefile>=0.47,<1               # legacy .doc / .ppt containers
pillow>=11,<13                 # image validation
rapidocr-onnxruntime>=1.3,<2   # OCR for PNG/JPG (optional: without it images are stored, not indexed, and the UI says so)
chardet>=5,<8                  # text encoding detection (e.g. Windows-1256 Arabic)
